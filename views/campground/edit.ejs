<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit</title>
</head>
<body>
    <h1>Edit <%= foundcamp.title %> </h1>
    <form action="/campgrounds/<%= foundcamp._id %>?_method=PUT" method="POST">
        <div class="title">
            <input type="text" placeholder="title" name="campground[title]" value="<%= foundcamp.title %>">
        </div>
        <div class="location">
            <input type="text" placeholder="location" name="campground[location]" value="<%= foundcamp.location %>">
        </div>
        <button type="submit">Update!</button>
    </form>
</body>
</html>